import { Section, Block } from '../components/Section';
import { Code, CodeBlock } from '../components/Code';

export function Sampling() {
  return (
    <Section
      id="sampling"
      eyebrow="IV · Выход"
      title="Sampling vs argmax"
      number="22"
    >
      <Block kind="intro">
        <p>
          У нас на руках полное распределение вероятностей по 151 936 токенам.
          Из него надо выбрать <i>один</i> следующий токен. Способов несколько,
          и они дают разное поведение модели.
        </p>
      </Block>

      <Block kind="words">
        <h4>Argmax (greedy)</h4>
        <p>
          Самый прямой способ: взять токен с самой высокой вероятностью.{' '}
          <Code>argmax(probs)</Code> = 2403 = «All». Это <b>детерминированный</b>{' '}
          выбор: на одних и тех же входах всегда одинаковый выход. Используется
          когда нужна повторяемость — для сравнений, бенчмарков, отладки.
        </p>
        <h4>Random sampling (по распределению)</h4>
        <p>
          Сделать честную выборку из распределения: токен i с вероятностью p_i.
          Тогда «All» выпадет в ≈ 49% случаев, «the» в ≈ ?%, и т.д. Используется
          когда хочется разнообразие в генерации — модель не повторяет один и тот
          же ответ, текст становится «живее».
        </p>
        <h4>Temperature</h4>
        <p>
          Перед softmax делят logits на температуру{' '}
          <span className="glyph"><span className="glyph__symbol">τ</span><span className="glyph__name"> (тау)</span></span>:
        </p>
        <CodeBlock lang="python">
{`probs = softmax(logits / temperature)
# τ = 1.0 — обычное распределение
# τ < 1.0 — острее, вероятности концентрируются на топ-кандидатах
# τ > 1.0 — мягче, распределение размывается, появляются неожиданные кандидаты`}
        </CodeBlock>
        <p>
          При τ → 0 sampling вырождается в argmax. При τ → ∞ распределение
          стремится к равномерному.
        </p>
        <h4>Top-K sampling</h4>
        <p>
          Берём только top-K кандидатов (например, K = 50), нормируем их
          вероятности (так чтобы сумма была 1 на этих K), и выбираем из этого
          подраспределения. Отрезает «длинный хвост» из миллионов
          бессмысленных токенов с очень низкой вероятностью.
        </p>
        <h4>Top-P (nucleus) sampling</h4>
        <p>
          Берём минимальный набор top-кандидатов, чьи вероятности в сумме дают
          по крайней мере P (например, P = 0.9). Это адаптивный top-K: на
          распределении с одним явным лидером возьмём 1–3 кандидата; на
          плоском распределении возьмём десятки.
        </p>
        <h4>Что выбрать на практике</h4>
        <p>
          Зависит от задачи:
        </p>
        <ul>
          <li><b>Точные ответы</b> (математика, факты, код): argmax или температура 0.0.</li>
          <li><b>Креативные тексты</b> (художка, диалоги, мозговой штурм): температура 0.7–1.0, top-p 0.9.</li>
          <li><b>Чат-помощник</b>: что-то посередине, обычно темп. 0.7, top-p 0.9.</li>
        </ul>
        <h4>Что мы будем считать «правильным» в этом разборе</h4>
        <p>
          В наших данных следующий токен — это просто argmax: «All». Но если бы
          модель занималась генерацией с темп = 1.0, в 49% случаев она бы тоже
          сказала «All», в остальных могла бы сказать «the», «·a» и т.д. Все
          эти варианты — корректные продолжения промпта.
        </p>
      </Block>

      <Block kind="real">
        <p>
          Тензоры в нашем репо посчитаны для argmax (greedy decoding). Поэтому
          <Code> autoregression.json</Code> — это всегда самый вероятный следующий
          токен на каждом шаге. Если бы мы запустили sampling, последовательность
          могла бы пойти по другому пути.
        </p>
      </Block>
    </Section>
  );
}
