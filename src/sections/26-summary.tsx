import { Section, Block } from '../components/Section';
import { Code } from '../components/Code';

export function Summary() {
  return (
    <Section
      id="summary"
      eyebrow="VI · Сводка и приложения"
      title="Средняя сводка всего процесса"
      number="25"
    >
      <p>
        Если ты прочитал всё подробное объяснение выше — этот раздел для того,
        чтобы освежить общую картину «без деталей, но с пониманием». Если же
        пришёл сразу сюда — не страшно: можно использовать этот раздел как карту
        и из него уходить в конкретные секции по ссылкам.
      </p>

      <Block kind="callout" label="Полный pipeline в 12 шагах">
        <ol>
          <li>
            <b><a href="#tokenize">BPE-токенизация</a></b>: текст «Reaching ATH (»
            режется на 4 кусочка по статистике обучения: <Code>Re | aching | ATH | (</Code>.
            Это решение принимает не модель, а заранее построенный словарь.
          </li>
          <li>
            <b><a href="#token-ids">ID токенов</a></b>: каждому кусочку соответствует
            свой номер в вокабуляре из 151 936 строк. Получаем 4 числа: 693, 11829, 87089, 320.
            Текст с этого момента превращается в массив целых чисел.
          </li>
          <li>
            <b><a href="#embedding">Embedding lookup</a></b>: каждый ID — это
            индекс в большой таблице E (151 936 × 2048). Достаём 4 строки. Получаем
            4 вектора по 2048 чисел — это «значения» наших токенов в 2048-мерном
            пространстве модели.
          </li>
          <li>
            <b><a href="#residual-stream">Residual stream</a></b>: эти 4 столбика начинают
            «течь» через 36 одинаковых по форме блоков. Каждый блок не заменяет
            столбики, а <i>прибавляет</i> к ним свои поправки. Так информация
            не теряется, а накапливается.
          </li>
          <li>
            <b><a href="#rmsnorm">Внутри блока</a></b> — две одинаковые подсистемы:
            <i> attention</i> (где токены смотрят друг на друга) и <i>MLP</i>{' '}
            (где каждый токен «думает сам»), каждая со своей RMSNorm на входе
            и residual add'ом на выходе.
          </li>
          <li>
            <b><a href="#projections">Q, K, V и GQA</a></b>: вход attention'а
            проецируется через 3 матрицы. Q даёт 16 параллельных «запросов»,
            K/V — всего 2 «ответа», но каждый делится между 8 Q. Это GQA —
            экономный вариант многоголового внимания.
          </li>
          <li>
            <b><a href="#rope">RoPE</a></b>: Q и K вращаются на углы, зависящие
            от позиции токена. 64 пары координат на голову, у каждой пары своя
            частота. Это тонкий и красивый способ закодировать позицию через
            геометрию, а не отдельный вектор.
          </li>
          <li>
            <b><a href="#attention-scores">Attention</a></b>: для каждой Q-головы
            считаем dot product Q · K, делим на √128, маскируем будущее (causal),
            пропускаем через <a href="#softmax">softmax</a>. Получаем 16 матриц
            4×4 — кто на кого «смотрит» с какой долей.
          </li>
          <li>
            <b><a href="#attention-v">Считываем V</a></b>: каждая позиция получает
            взвешенную сумму V-векторов всех видимых ей позиций. Затем 16 голов
            склеиваются и пропускаются через <a href="#output-proj">W_O</a>, чтобы
            смешать их между собой. Прибавляем к residual stream.
          </li>
          <li>
            <b><a href="#mlp">MLP / SwiGLU</a></b>: для каждого токена отдельно
            расширяем 2048 → 11 008, пропускаем через хитрую SwiGLU-активацию,
            сжимаем обратно. Это самая параметрически тяжёлая часть блока.
            Прибавляем к residual stream — так заканчивается один блок.
          </li>
          <li>
            <b><a href="#layer-loop">36 раз подряд</a></b> — выходит финальное
            состояние h_36. <a href="#logit-lens">Logit lens</a> показывает как
            top-1 эволюционирует от слоя к слою: на L=0 это сам входной токен,
            на L=18 «бред», на L=36 — «All».
          </li>
          <li>
            <b><a href="#unembedding">Unembedding и финальный softmax</a></b>:
            берём состояние последнего токена, нормируем (final RMSNorm), умножаем
            на E^T — получаем 151 936 logits. Softmax превращает их в вероятности.
            Top-1 = «All» с p ≈ 49%. Это и есть предсказанный следующий токен.
          </li>
        </ol>
      </Block>

      <Block kind="callout" label="Что важно запомнить">
        <ul>
          <li>
            <b>Forward pass детерминирован.</b> На одних и тех же входах — те же
            выходы. Все «случайности» в генерации появляются позже, при <a href="#sampling">sampling</a>.
          </li>
          <li>
            <b>Архитектура повторяющаяся.</b> 36 раз — один и тот же блок с одним
            и тем же набором операций, только с разными весами на каждом слое.
          </li>
          <li>
            <b>Residual stream — главная магистраль.</b> Все блоки только
            прибавляют к нему свои выводы. Информация не теряется.
          </li>
          <li>
            <b>Attention делает связи между токенами.</b> MLP делает «знания
            каждого токена». Эти две подсистемы дополняют друг друга в каждом
            блоке.
          </li>
          <li>
            <b>RoPE — позиция через геометрию.</b> Не отдельный вектор, а
            вращение пар координат — это даёт модели чувство относительной
            позиции «бесплатно».
          </li>
          <li>
            <b>В конце — выстрел в словарь.</b> Финальный residual stream
            «спрашивается» у каждой строки embedding-матрицы насколько он на неё
            похож. Самый похожий — следующий токен.
          </li>
          <li>
            <b>Autoregression — это просто повторение.</b> Чтобы сгенерировать
            «All Time High», модель сделала один полный forward pass, добавила «All»
            в промпт, сделала ещё один полный forward pass, добавила «·Time», и так
            далее. KV-cache спасает от пересчёта прежних шагов.
          </li>
        </ul>
      </Block>

      <Block kind="note" label="Если ты дойдёшь до визуализации">
        <p>
          Каждый из 12 шагов выше — это потенциальный «нолановский beat». Самые
          кинематографичные:
        </p>
        <ul>
          <li>BPE-нарезка с подсветкой границ (шаг 1).</li>
          <li>Скролл-лента словаря на 151 936 строк с остановкой на нужных ID (шаг 2).</li>
          <li>4 столбика по 2048 чисел, которые «вытягиваются» из словаря (шаг 3).</li>
          <li>RoPE-циферблаты на разных частотах (шаг 7) — самое механически-красивое.</li>
          <li>Линии attention между столбиками с весами (шаг 8).</li>
          <li>Logit lens timeline — как top-1 эволюционирует от L=0 до L=36 (шаг 11).</li>
          <li>Финальный «выстрел» в 151 936 строк словаря с подсветкой «All» (шаг 12).</li>
        </ul>
      </Block>
    </Section>
  );
}
