import { Section, Block } from '../components/Section';
import { Code } from '../components/Code';

export function Intro() {
  return (
    <Section id="intro" eyebrow="0 · Введение" title="Что мы вообще смотрим">
      <p>
        Мы открыли крышку у языковой модели <Code>Qwen-2.5-3B</Code>, дали ей одну строку
        текста — <Code>"Reaching ATH ("</Code> — и хотим разобраться, что именно она
        с этой строкой делает, прежде чем выдать первый следующий токен (это будет
        слово <Code>"All"</Code>).
      </p>
      <p>
        Модель не «понимает» язык в человеческом смысле. Она выполняет фиксированную
        последовательность арифметических операций над числами. Эта последовательность
        называется <b>forward pass</b> — «прямой проход». Если на вход подать одни
        и те же числа, на выход всегда придут ровно те же числа: процесс полностью
        детерминированный.
      </p>

      <Block kind="callout" label="Что мы будем делать с каждым шагом">
        <p>Для каждой операции внутри модели я даю шесть слоёв объяснения:</p>
        <ol>
          <li>
            <b>Что это и зачем</b> — место операции в общем pipeline, что было на
            входе, что будет на выходе.
          </li>
          <li>
            <b>Сырая формула</b> — полная математическая запись со всеми символами.
            Каждый незнакомый символ в первом упоминании я пишу с транскрипцией:
            например <span className="glyph"><span className="glyph__symbol">θ</span><span className="glyph__name"> (тета)</span></span>.
          </li>
          <li>
            <b>Псевдокод</b> — та же операция, переписанная как Python-подобный код
            с осмысленными именами.
          </li>
          <li>
            <b>Подробное словесное объяснение</b> — что делает каждая часть формулы,
            почему именно так, что произошло бы, если бы мы её убрали.
          </li>
          <li>
            <b>Интерактив</b> — где можно — небольшой виджет, который позволяет
            покрутить параметры и посмотреть как меняется результат.
          </li>
          <li>
            <b>На наших числах</b> — реальные размерности и иногда конкретные числа
            из forward-pass'а Qwen-2.5-3B на нашем промпте.
          </li>
        </ol>
      </Block>

      <p>
        В конце есть <b>средняя сводка</b> всего процесса (короче чем сам разбор, но
        достаточная чтобы держать общую картину в голове) и <b>глоссарий</b> со всеми
        терминами и транскрипциями.
      </p>

      <Block kind="note" label="Откуда числа">
        <p>
          В этом репозитории, в <Code>public/data/tensors/</Code>, лежат настоящие
          тензоры (массивы чисел) реального forward-pass'а Qwen-2.5-3B на промпте
          «Reaching ATH (»: эмбеддинги, residual stream после каждого из 36 слоёв,
          attention-веса каждого слоя × каждой головы, финальные logits и
          вероятности по всем 151 936 словам словаря. Когда я пишу «на наших
          числах» — я беру их именно оттуда.
        </p>
      </Block>
    </Section>
  );
}
