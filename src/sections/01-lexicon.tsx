import { Section, Block } from '../components/Section';

export function Lexicon() {
  return (
    <Section id="lexicon" eyebrow="0 · Введение" title="Базовый словарь терминов">
      <p>
        Здесь — короткие объяснения слов, которые будут встречаться повсюду. Каждое
        будет потом разобрано в своём разделе, но удобно сразу иметь общую карту.
      </p>

      <dl className="defs">
        <dt>Токен</dt>
        <dd>
          Кусочек текста, на который модель режет вход. Не обязательно слово — может
          быть приставка, суффикс, символ. Для нашего промпта получится 4 токена.
        </dd>

        <dt>ID токена</dt>
        <dd>
          Целое число — порядковый номер токена в словаре модели. У Qwen-2.5-3B словарь
          размера <b>151 936</b>: каждый токен имеет ID в диапазоне 0…151 935.
        </dd>

        <dt>Эмбеддинг</dt>
        <dd>
          Вектор фиксированной длины (<b>2048</b> чисел в нашей модели), которым
          представлен токен внутри сети. Это «значение» токена в высокоразмерном
          пространстве.
        </dd>

        <dt>Hidden state</dt>
        <dd>
          Состояние модели на каждом из 36 слоёв — для каждого токена это всё ещё
          вектор длины 2048, но уже изменённый предыдущими слоями.
        </dd>

        <dt>Residual stream</dt>
        <dd>
          «Поток» эмбеддингов через все слои. Каждый блок не <i>заменяет</i> вектор,
          а <i>прибавляет</i> к нему свой выход. Идея — информация течёт сквозь сеть
          и постепенно обогащается, а не пересоздаётся.
        </dd>

        <dt>Слой / блок (transformer block)</dt>
        <dd>
          Одна повторяющаяся единица архитектуры. В Qwen-2.5-3B их <b>36</b>. Каждый
          блок содержит attention-часть (где токены смотрят друг на друга) и MLP-часть
          (где каждый токен думает сам по себе).
        </dd>

        <dt>Голова внимания (attention head)</dt>
        <dd>
          Внутри одного слоя attention делается параллельно несколькими «головами» —
          по сути, разными точками зрения на те же токены. У Qwen-2.5-3B 16 query-голов
          и 2 key/value-головы (об этом — в разделе про GQA).
        </dd>

        <dt>Q, K, V</dt>
        <dd>
          Query (запрос), Key (ключ), Value (значение) — три проекции одного токена,
          которые используются в attention. Q отвечает «что я ищу», K — «чем я могу
          быть найден», V — «что я скажу, если меня выберут».
        </dd>

        <dt>RoPE (Rotary Position Embedding)</dt>
        <dd>
          Способ закодировать позицию токена не отдельным вектором, а через
          вращение пар координат Q и K на углы, зависящие от позиции. Об этом — целый
          отдельный раздел.
        </dd>

        <dt>Softmax</dt>
        <dd>
          Функция, превращающая произвольный набор чисел в распределение
          вероятностей: сумма выходов всегда равна 1. Используется и в attention,
          и в финальном предсказании следующего токена.
        </dd>

        <dt>Logits</dt>
        <dd>
          Сырые «голоса за каждое слово» из словаря — вектор длины 151 936,
          который модель выдаёт на самом конце. Softmax превращает их в вероятности.
        </dd>

        <dt>Argmax</dt>
        <dd>
          Операция «индекс самого большого значения». Если мы хотим самый
          вероятный следующий токен — берём argmax от logits.
        </dd>

        <dt>Autoregression</dt>
        <dd>
          Способ генерации текста по одному токену за раз: модель
          предсказывает один следующий токен, его дописывают к промпту и снова
          гонят весь forward pass.
        </dd>
      </dl>

      <Block kind="note" label="Маленькая подсказка по чтению">
        <p>
          Любой математический символ в этом документе при первом упоминании
          сопровождается транскрипцией: <span className="glyph"><span className="glyph__symbol">θ</span><span className="glyph__name"> (тета)</span></span>,
          <span className="glyph"> <span className="glyph__symbol">α</span><span className="glyph__name"> (альфа)</span></span>,
          <span className="glyph"> <span className="glyph__symbol">Σ</span><span className="glyph__name"> (сигма)</span></span>,
          <span className="glyph"> <span className="glyph__symbol">√</span><span className="glyph__name"> (квадратный корень)</span></span>, и так далее.
          Если вы потеряли где какой символ — в конце есть глоссарий.
        </p>
      </Block>
    </Section>
  );
}
