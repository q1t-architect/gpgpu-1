import { Section, Block } from '../components/Section';
import { Code, CodeBlock } from '../components/Code';
import { TokenizerView } from '../widgets/TokenizerView';

export function Tokenize() {
  return (
    <Section id="tokenize" eyebrow="I · Вход" title="Текст → токены (BPE)" number="1">
      <Block kind="intro">
        <p>
          Первое что делает модель — режет нашу строку текста на кусочки, которые она
          умеет «узнавать». Эти кусочки называются <b>токенами</b>. Алгоритм называется
          BPE — <i>Byte Pair Encoding</i> (бай-пэр энкодинг, «кодирование байтовыми
          парами»). Результатом будет упорядоченный список из 4 токенов.
        </p>
      </Block>

      <Block kind="formula">
        <p>
          Формально BPE — это <b>жадный детерминированный алгоритм</b>. Модель имеет
          фиксированный словарь V (вэ) из 151 936 токенов и фиксированный список merge-правил
          M (эм) — это пары символов, которые в обучении часто встречались вместе и которые
          нужно склеивать в один токен. Алгоритм для строки s (эс):
        </p>
        <CodeBlock lang="pseudo">
{`tokens = list(s)            # начали с отдельных символов или байтов
while True:
    # найти пару соседних токенов с самым ранним merge-правилом
    pair = argmin_{(a,b) in tokens}  rank(M, a, b)
    if pair is None: break
    tokens = apply_merge(tokens, pair)`}
        </CodeBlock>
        <p>
          Здесь <Code>rank(M, a, b)</Code> — это позиция пары (a, b) в списке merge-правил
          (раньше = лучше); если такой пары в правилах нет, она не сливается. Алгоритм
          останавливается когда сливать больше нечего. То что осталось — финальные
          токены.
        </p>
      </Block>

      <Block kind="pseudo">
        <CodeBlock lang="python">
{`from transformers import AutoTokenizer

tok = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-3B")
ids = tok.encode("Reaching ATH (")
# ids → [693, 11829, 87089, 320]
# decoded:  "Re"  "aching"  " ATH"  " ("`}
        </CodeBlock>
      </Block>

      <Block kind="words">
        <h4>Зачем вообще резать на токены</h4>
        <p>
          Альтернативы — резать на отдельные буквы (тогда последовательность была бы
          слишком длинной и медленной) или работать со словами целиком (тогда словарь
          был бы огромным, и модель не справилась бы со словами, которых нет в
          словаре). BPE — компромисс: частые куски (например, окончания, артикли,
          распространённые слова) хранятся как отдельные токены, редкие — как
          несколько подкусочков.
        </p>
        <h4>Почему именно эти границы</h4>
        <p>
          Слово <Code>Reaching</Code> для Qwen разваливается на <Code>Re</Code> +{' '}
          <Code>aching</Code> потому что в обучающих данных модели английский текст
          с подобной формой встречался часто как <Code>Re-/-aching</Code>, а целое
          <Code> Reaching</Code> — относительно реже. <Code> ATH</Code> зато выжило
          как один токен — это аббревиатура с осмысленным контекстом (стоковые
          торги: «All Time High»). А <Code>{' ('}</Code> — отдельный знак пунктуации
          с контекстом «начало скобки», у него свой ID.
        </p>
        <h4>Что было бы, если убрать токенизацию</h4>
        <p>
          Если бы мы скармливали модели сырые байты, ей пришлось бы узнавать
          частотные комбинации заново на каждом forward-pass'е. BPE «зашит» в
          обучение модели — он позволяет ей сразу работать на уровне осмысленных
          кусочков и держит длину последовательности маленькой.
        </p>
        <h4>Важная деталь — пробелы</h4>
        <p>
          У Qwen, как у LLaMA-семейства, пробел приклеивается к <i>следующему</i>{' '}
          токену, а не к предыдущему. Поэтому у нас есть{' '}
          <Code>{' ATH'}</Code> (с лидирующим пробелом) и{' '}
          <Code>{' ('}</Code>, но нет «висящего» отдельного пробела между ними. Это
          не косметика — это задано BPE-словарём и важно для воспроизводимости.
        </p>
      </Block>

      <Block kind="demo">
        <TokenizerView />
      </Block>

      <Block kind="real">
        <p>
          На нашем промпте «Reaching ATH (» модель получает 4 токена с ID{' '}
          <Code>693, 11829, 87089, 320</Code>. С этого момента и до самого конца
          forward-pass'а модель будет работать с этими 4 числами (а в следующем
          разделе — с тем что за этими числами стоит на самом деле).
        </p>
      </Block>
    </Section>
  );
}
