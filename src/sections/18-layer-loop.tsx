import { Section, Block } from '../components/Section';
import { CodeBlock } from '../components/Code';

export function LayerLoop() {
  return (
    <Section
      id="layer-loop"
      eyebrow="III · 36 слоёв"
      title="Что меняется от слоя к слою"
      number="17"
    >
      <Block kind="intro">
        <p>
          Один блок мы уже разобрали в максимальной глубине. Теперь весь forward
          pass — это просто его 36-кратное повторение. Здесь почти нечего добавить
          по математике: те же формулы. Но <i>веса разные</i>, поэтому каждый блок
          выполняет свою функциональную роль.
        </p>
      </Block>

      <Block kind="pseudo">
        <CodeBlock lang="python">
{`# Полный forward pass через все 36 слоёв
h = E[ids]                              # (4, 2048) — после embedding

for layer_idx in range(36):
    h = transformer_block(h, weights[layer_idx])

# h теперь — это hidden_states[36]
final_state = rms_norm(h, weights["final_norm"])
logits = final_state @ E.T              # unembedding (см. раздел 19)
probs = softmax(logits[-1])             # вероятности по 151 936 токенам`}
        </CodeBlock>
      </Block>

      <Block kind="words">
        <h4>Что меняется от слоя к слою — на пальцах</h4>
        <p>
          Сами операции (attention с RoPE, softmax, V-комбинация, MLP, residual
          add'ы) повторяются один в один. Меняются <i>веса</i> — у каждого слоя
          свой набор W_Q, W_K, W_V, W_O, W_g, W_u, W_d, и два g-вектора для
          RMSNorm. Это значит, что:
        </p>
        <ul>
          <li>
            На разных слоях <b>разный pattern attention</b>. На раннем слое
            модель смотрит «куда подскажут низкоуровневые признаки» (соседние
            токены, синтаксис). На глубоком слое — на семантически связанные
            токены, не обязательно соседние.
          </li>
          <li>
            На разных слоях <b>MLP «думает о разном»</b>. Эмпирически: в нижних
            MLP лежат паттерны вроде «после открывающей скобки часто идёт цифра»;
            в верхних — фактоидные знания вроде «после ‘All Time’ обычно идёт ‘High’».
          </li>
        </ul>
        <h4>Эмпирические наблюдения о послойной эволюции</h4>
        <ol>
          <li>
            <b>Слои 0–8 — копирование и нормализация.</b> Residual stream ещё
            почти такой же, как embedding'и, плюс мелкие синтаксические правки.
            Если применить unembedding в этой точке, модель будет предсказывать
            что-то очень близкое к самим входным токенам или их вариациям.
          </li>
          <li>
            <b>Слои 9–24 — формирование контекста.</b> Attention начинает
            активно перетаскивать информацию между токенами. Появляются
            «прокси-предсказания»: модель уже примерно знает что хочет
            предсказать.
          </li>
          <li>
            <b>Слои 25–35 — финализация.</b> Концентрация внимания на одном-двух
            ключевых токенах, повышение уверенности в одном top-1.
          </li>
          <li>
            <b>Слой 36 — финальный.</b> Состояние, готовое к unembedding.
          </li>
        </ol>
        <p>
          В следующем разделе (logit lens) мы посмотрим этот эффект на наших
          реальных данных — увидим как top-1 предсказание меняется от слоя к
          слою.
        </p>
        <h4>Сколько раз срабатывает каждая операция за весь forward pass</h4>
        <ul>
          <li>RMSNorm: 73 раза</li>
          <li>Матричное умножение Q/K/V: 36 × 3 = 108 (на четырёх токенах одновременно)</li>
          <li>Применение RoPE: 36 × (16 + 2) = 648 раз (по числу голов на слой)</li>
          <li>Softmax внутри attention: 36 × 16 × 4 = 2 304 раза (по строке q каждой матрицы)</li>
          <li>MLP полностью (gate + up + down): 36 раз</li>
          <li>Residual add: 72 раза</li>
        </ul>
        <p>
          В сумме это сотни матричных умножений, каждое с миллионами
          операций — отсюда требование к GPU.
        </p>
        <h4>Почему именно 36</h4>
        <p>
          Это гиперпараметр архитектуры. У Qwen-2.5-7B их 28 (более широкие,
          но менее глубокие); у Qwen-2.5-72B — 80. Соотношение «глубина vs
          ширина» подбирается эмпирически: для одинакового бюджета параметров
          разные конфигурации дают разные результаты, и 3B-вариант сошёлся
          именно на 36.
        </p>
      </Block>
    </Section>
  );
}
