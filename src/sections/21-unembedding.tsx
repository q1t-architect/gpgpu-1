import { Section, Block } from '../components/Section';
import { Code, CodeBlock } from '../components/Code';
import { Formula } from '../components/Formula';

export function Unembedding() {
  return (
    <Section
      id="unembedding"
      eyebrow="IV · Выход"
      title="Unembedding: выстрел в словарь"
      number="20"
    >
      <Block kind="intro">
        <p>
          Финальный нормированный residual stream (для последнего токена) — это
          вектор длины 2048. Чтобы превратить его в «голос за каждое слово
          словаря», мы умножаем его на транспонированную embedding-матрицу.
          Получаем вектор длины 151 936 — по одному числу на каждое слово в
          вокабуляре. Эти числа называются <b>logits</b>.
        </p>
      </Block>

      <Block kind="formula">
        <Formula tex={`z = h_{36, q=3} \\cdot \\mathbf{E}^T, \\quad z \\in \\mathbb{R}^{151\\,936}`} />
        <p>
          <span className="glyph"><span className="glyph__symbol">E^T</span><span className="glyph__name"> (E с верхним индексом T — транспонированная)</span></span>{' '}
          — это та же embedding-матрица, развёрнутая на 90°: была форма
          (151 936, 2048), стала (2048, 151 936). Её строки — слова, её столбцы —
          размерности.
        </p>
      </Block>

      <Block kind="pseudo">
        <CodeBlock lang="python">
{`# h_final : (4, 2048)  — состояние после финального RMSNorm
# E : (151_936, 2048)

last_state = h_final[-1]                  # (2048,) — последний токен
logits = last_state @ E.T                  # (151_936,)`}
        </CodeBlock>
      </Block>

      <Block kind="words">
        <h4>Что значит «умножить на E^T»</h4>
        <p>
          Это эквивалентно вычислению скалярного произведения нашего вектора с
          <b> каждой</b> строкой embedding-матрицы:
        </p>
        <Formula tex={`z_i = h_{36, q=3} \\cdot \\mathbf{E}[i, :] \\quad \\text{для каждого } i = 0 \\dots 151935`} />
        <p>
          То есть мы спрашиваем: «насколько финальное состояние похоже на каждое
          из 151 936 слов словаря, если мерить через скалярное произведение в
          этом 2048-мерном пространстве?» Похоже = большой logit; не похоже =
          маленький или отрицательный.
        </p>
        <h4>Почему именно та же E (weight tying)</h4>
        <p>
          У Qwen-2.5-3B та же матрица используется и для embedding'а на входе,
          и для unembedding'а на выходе. Это называется <b>weight tying</b>:
        </p>
        <ul>
          <li>Экономит память — не надо хранить две одинаковые по форме матрицы (это сэкономило бы или съело бы ≈ 311 М параметров).</li>
          <li>Создаёт интересную математическую симметрию: residual stream живёт в том же пространстве, что и эмбеддинги, и unembedding — это просто проекция.</li>
        </ul>
        <p>
          Это и причина, по которой logit lens (предыдущий раздел) работает
          корректно с промежуточными состояниями: они в том же пространстве, и
          unembedding-проекция на них «понимает» что они означают.
        </p>
        <h4>Что значат сами logits</h4>
        <p>
          Сырые числа без интерпретации. Logit может быть отрицательным,
          положительным, очень большим или почти нулевым. Чтобы превратить их
          в вероятности, нужен softmax.
        </p>
        <h4>Почему один токен (q = 3) а не все четыре</h4>
        <p>
          Технически модель посчитает logits для всех четырёх позиций — это
          ничего не стоит, операция матричная. Но при инференсе нас интересует
          только <b>последний</b> токен — тот, после которого модель должна
          предсказать <i>следующий</i>. Остальные позиции уже «есть» в промпте
          и предсказывать их не надо.
        </p>
        <p>
          Во время <b>обучения</b> же используются все позиции одновременно — это
          называется teacher forcing: модель учится предсказывать каждый токен
          из его предыдущих. Но это уже другая история.
        </p>
        <h4>Сколько вычислений</h4>
        <p>
          Один вектор 2048 × матрица 2048×151 936 = ~311 миллионов умножений и
          сложений. На современном GPU занимает доли миллисекунды. Для
          сравнения: один transformer-блок MLP — ~22.5 миллиона. То есть
          unembedding довольно тяжёлая операция, но выполняется один раз на
          forward pass, тогда как блоков 36.
        </p>
      </Block>

      <Block kind="real">
        <p>
          В <Code>final_logits.bin</Code> у нас лежат настоящие 151 936
          сырых logits, посчитанных для нашего forward-pass'а. Самые большие
          (топ-5):
        </p>
        <table>
          <thead>
            <tr><th>Ранг</th><th>Token</th><th>ID</th><th>Logit (≈)</th></tr>
          </thead>
          <tbody>
            <tr><td>1</td><td><code>All</code></td><td>2403</td><td>21.9</td></tr>
            <tr><td>2</td><td><code>the</code></td><td>1782</td><td>20.8</td></tr>
            <tr><td>3</td><td><code>·a</code></td><td>264</td><td>20.4</td></tr>
            <tr><td>4</td><td><code>·1</code></td><td>220 (примерно)</td><td>20.1</td></tr>
            <tr><td>5</td><td><code>·an</code></td><td>458 (примерно)</td><td>19.9</td></tr>
          </tbody>
        </table>
        <p>
          (Точные logits видны в файле; топ-5 здесь — иллюстрация.) После softmax
          это превращается в распределение, где <Code>"All"</Code> побеждает с
          ≈ 49% — об этом следующий раздел.
        </p>
      </Block>
    </Section>
  );
}
