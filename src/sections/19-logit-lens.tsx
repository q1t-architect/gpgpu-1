import { Section, Block } from '../components/Section';
import { Code, CodeBlock } from '../components/Code';
import { Formula } from '../components/Formula';
import { LogitLensViewer } from '../widgets/LogitLensViewer';

export function LogitLensSection() {
  return (
    <Section
      id="logit-lens"
      eyebrow="III · 36 слоёв"
      title="Logit lens: что модель «думает» на каждом слое"
      number="18"
    >
      <Block kind="intro">
        <p>
          <b>Logit lens</b> (логит-ленз, «логит-линза») — техника интерпретации,
          которая позволяет «остановить» модель на любом слое ℓ и посмотреть, что
          она <i>уже уверена</i> предсказывать как следующий токен. Способ простой:
          применить финальные операции (RMSNorm + unembedding + softmax) к
          промежуточному residual stream <Code>h_ℓ</Code> вместо финального{' '}
          <Code>h_36</Code>. Получаем «срез» вероятностей по всему словарю — но
          построенный не из настоящего конца модели, а из её середины.
        </p>
      </Block>

      <Block kind="formula">
        <Formula tex={`p_\\ell = \\text{softmax}\\big(\\text{RMSNorm}_{\\text{final}}(h_{\\ell, q=3}) \\cdot \\mathbf{E}^T\\big)`} />
        <p>
          Здесь <Code>h_{`{ℓ, q=3}`}</Code> — residual stream на слое ℓ для последнего
          токена (q = 3, токен <Code>"·("</Code>). <Code>E^T</Code> — транспонированная
          embedding-матрица: используется как «unembedding» (см. раздел 20).
          На выходе — распределение вероятностей по всем 151 936 токенам словаря.
        </p>
      </Block>

      <Block kind="pseudo">
        <CodeBlock lang="python">
{`def logit_lens(h, layer_idx, weights, E):
    # h : (37, 4, 2048) — residual stream после каждого слоя
    state = h[layer_idx, -1]                      # (2048,) последний токен
    state = rms_norm(state, weights["final_norm"])
    logits = state @ E.T                          # (151_936,)
    probs = softmax(logits)
    return top_k(probs, k=10)

# Результат на слое 0:  top1 = "·("    p = 1.000
# Результат на слое 18: top1 = "·15"   p = 0.029   (топ ещё рассеян)
# Результат на слое 36: top1 = "All"   p = 0.492`}
        </CodeBlock>
      </Block>

      <Block kind="words">
        <h4>Зачем это нужно</h4>
        <p>
          Logit lens даёт интерпретируемое окно во внутренние состояния модели.
          Без него residual stream — это просто 2048 чисел, ничего не говорящих
          человеку напрямую. С logit lens мы переводим эти числа в «ближайший
          понятный токен» — то, к чему текущее состояние ближе всего в смысле
          dot product с embedding-матрицей.
        </p>
        <h4>Почему это «честно»</h4>
        <p>
          Embedding-матрица <Code>E</Code> и unembedding-матрица в Qwen-2.5-3B —
          одна и та же (это называется weight tying, «связывание весов»: модель
          использует <Code>E</Code> и для входа, и для выхода). Это значит, что
          residual stream на любом слое уже живёт в том же векторном пространстве,
          что и эмбеддинги; logit lens просто измеряет «насколько текущее состояние
          похоже на каждое из слов словаря».
        </p>
        <h4>Чему верить, а чему нет</h4>
        <p>
          На раннем слое logit lens может выдавать осмысленные слова — но это
          не значит, что модель <i>сейчас</i> их предсказывает. Это значит лишь,
          что <i>если бы мы остановили обработку здесь и применили unembedding</i>,
          мы получили бы такие распределения. Реальная финальная функция работает
          только после всех 36 блоков. Logit lens — это диагностическое окно,
          а не «настоящая мысль» модели.
        </p>
        <h4>Зачем смотреть на это в нашем случае</h4>
        <p>
          Это самый прямой способ показать <i>эволюцию состояния</i> модели на нашем
          конкретном промпте. Мы увидим:
        </p>
        <ul>
          <li>
            На L = 0 top-1 это сам входной токен с вероятностью ≈ 1 (модель ничего
            ещё не сделала).
          </li>
          <li>
            На L = 5–15 top-1 «бредит» — какие-то соседние токены, цифры, вариации.
            Модель ещё не понимает что хочет.
          </li>
          <li>
            На L = 25–32 появляются осмысленные кандидаты: <Code>All</Code>,{' '}
            <Code>the</Code>, <Code>·a</Code>.
          </li>
          <li>
            На L = 36 финальный top-1 — <Code>All</Code> с вероятностью 49.2%.
          </li>
        </ul>
        <h4>Важная техническая деталь — где брать состояние</h4>
        <p>
          Logit lens применяется к <i>последнему токену</i> в последовательности
          (q = 3 у нас). Это потому, что предсказывать «следующий после промпта»
          токен умеет только последняя позиция (она «видела» все предыдущие). На
          ранних слоях это ещё не очень информативно (causal attention позволил
          ей увидеть только себя и предыдущих); на поздних — вся накопленная
          информация уже стянута в последнее состояние.
        </p>
      </Block>

      <Block kind="demo">
        <LogitLensViewer />
      </Block>

      <Block kind="real">
        <p>
          Файл <Code>logit_lens.json</Code> содержит топ-15 предсказаний на каждом
          из 37 слоёв (включая L=0 и L=36) — реальные числа, посчитанные при
          подготовке тензоров на машине с настоящей моделью. Виджет выше — это
          именно эти данные. Двигай ползунок, чтобы увидеть как меняется top-1.
        </p>
      </Block>
    </Section>
  );
}
